# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "portable_atomic_enum"
version = "0.3.1"
authors = [
    "Thomas Bächler <thomas.baechler@gmx.de>",
    "Dániel Buga <bugadani@gmail.com>",
]
description = "An attribute to create an portable atomic wrapper around a C-style enum"
readme = "README.md"
keywords = [
    "atomic",
    "enum",
]
categories = ["concurrency"]
license = "MIT"
repository = "https://github.com/bugadani/portable_atomic_enum"

[dependencies.portable-atomic]
version = "1.5"
optional = true
default-features = false

[dependencies.portable_atomic_enum_macros]
version = "0.2.1"

[features]
portable-atomic = ["dep:portable-atomic"]
